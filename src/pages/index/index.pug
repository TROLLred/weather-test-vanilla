extends ../../app/pug/layout

block title
    | Главная

block content
    main(data-barba="container" data-barba-namespace="common" data-page-namespace="index")
        +lead-section(dataJson.index.leadSection)
        +events-grid(dataJson.index.eventsGrid)
